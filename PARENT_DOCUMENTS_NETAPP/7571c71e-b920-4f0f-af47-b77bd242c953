
                   <p style="text-align: center">
                       <b>Title:</b> No title detected<br>
                       <b>Bounding box:</b> x1=134, y1=100, x2=1566, y2=2038
                   </p>
                   <div align="center"><table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>0</th>
      <th>1</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>Operation</td>
      <td>Implementation</td>
    </tr>
    <tr>
      <th>1</th>
      <td>CreateBucket</td>
      <td>Creates a new bucket. By creating the bucket, you become the bucket owner.\n• Bucket names must comply with the following rules:\n◦Must be unique across each StorageGRID system (not just unique within\nthe tenant account).\n◦Must be DNS compliant.\n◦Must contain at least 3 and no more than 63 characters.\n◦Can be a series of one or more labels, with adjacent labels separated by\na period. Each label must start and end with a lowercase letter or a\nnumber and can only use lowercase letters, numbers, and hyphens.\n◦Must not look like a text-formatted IP address.\n◦Should not use periods in virtual hosted style requests. Periods will cause\nproblems with server wildcard certificate verification.\n• region; however, you can By default, buckets are created in the us-east-1\nuse the request element in the request body to LocationConstraint\nelement, specify a different region. When using the LocationConstraint\nyou must specify the exact name of a region that has been defined using the\nGrid Manager or the Grid Management API. Contact your system\nadministrator if you don’t know the region name you should use.\nNote: An error will occur if your CreateBucket request uses a region that has\nnot been defined in StorageGRID.\n• You can include the request x-amz-bucket-object-lock-enabled\nUse S3 REST header to create a bucket with S3 Object Lock enabled. See\nAPI to configure S3 Object Lock.\nYou must enable S3 Object Lock when you create the bucket. You can’t add\nor disable S3 Object Lock after a bucket is created. S3 Object Lock requires\nbucket versioning, which is enabled automatically when you create the\nbucket.</td>
    </tr>
    <tr>
      <th>2</th>
      <td>DeleteBucket</td>
      <td>Deletes the bucket.</td>
    </tr>
    <tr>
      <th>3</th>
      <td>DeleteBucketCors</td>
      <td>Deletes the CORS configuration for the bucket.</td>
    </tr>
    <tr>
      <th>4</th>
      <td>DeleteBucketEncryption</td>
      <td>Deletes the default encryption from the bucket. Existing encrypted objects remain\nencrypted, but any new objects added to the bucket aren’t encrypted.</td>
    </tr>
    <tr>
      <th>5</th>
      <td>DeleteBucketLifecycle</td>
      <td>Deletes the lifecycle configuration from the bucket. See Create S3 lifecycle\nconfiguration.</td>
    </tr>
    <tr>
      <th>6</th>
      <td>DeleteBucketPolicy</td>
      <td>Deletes the policy attached to the bucket.</td>
    </tr>
    <tr>
      <th>7</th>
      <td>DeleteBucketReplication</td>
      <td>Deletes the replication configuration attached to the bucket.</td>
    </tr>
  </tbody>
</table></div>
                   <hr>
                