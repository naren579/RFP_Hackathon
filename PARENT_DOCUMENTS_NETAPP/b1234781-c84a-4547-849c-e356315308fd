
                   <p style="text-align: center">
                       <b>Title:</b> 4. Select Add rule and specify the following details to create one or more matching rules for the policy. Any
policy that you create should have at least one matching rule. Select Continue.<br>
                       <b>Bounding box:</b> x1=184, y1=760, x2=1566, y2=1676
                   </p>
                   <div align="center"><table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>0</th>
      <th>1</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>Field</td>
      <td>Description</td>
    </tr>
    <tr>
      <th>1</th>
      <td>Type</td>
      <td>Select the types of traffic that the matching rule applies to. Traffic types are\nbucket, bucket regex, CIDR, load balancer endpoint, and tenant.</td>
    </tr>
    <tr>
      <th>2</th>
      <td>Match value</td>
      <td>Enter the value that matches the selected Type.\n• Bucket: Enter one or more bucket names.\n• Bucket regex: Enter one or more regular expressions used to match a set\nof bucket names.\nThe regular expression is unanchored. Use the ^ anchor to match at the\nbeginning of the bucket name, and use the $ anchor to match at the end of\nthe name. Regular expression matching supports a subset of PCRE (Perl\ncompatible regular expression) syntax.\n• CIDR: Enter one or more IPv4 subnets, in CIDR notation, that matches the\ndesired subnet.\n• Load balancer endpoint: Select an endpoint name. These are the load\nbalancer endpoints you defined on the Configure load balancer endpoints.\n• Tenant: Tenant matching uses the access key ID. If the request does not\ncontain an access key ID (for example, anonymous access), then the\nownership of the bucket accessed is used to determine the tenant.</td>
    </tr>
  </tbody>
</table></div>
                   <hr>
                