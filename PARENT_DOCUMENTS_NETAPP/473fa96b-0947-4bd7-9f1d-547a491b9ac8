
                   <p style="text-align: center">
                       <b>Title:</b> No title detected<br>
                       <b>Bounding box:</b> x1=234, y1=100, x2=1566, y2=1530
                   </p>
                   <div align="center"><table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>0</th>
      <th>1</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>Field</td>
      <td>Description</td>
    </tr>
    <tr>
      <th>1</th>
      <td>NTP Role</td>
      <td>The Network Time Protocol (NTP) role of the grid node:\n• Select Automatic (default) to automatically assign the NTP role\nto the node. The Primary role will be assigned to Admin Nodes,\nStorage Nodes with ADC services, Gateway Nodes, and any\ngrid nodes that have non-static IP addresses. The Client role\nwill be assigned to all other grid nodes.\n• Select Primary to manually assign the Primary NTP role to the\nnode. At least two nodes at each site should have the Primary\nrole to provide redundant system access to external timing\nsources.\n• Select Client to manually assign the Client NTP role to the\nnode.</td>
    </tr>
    <tr>
      <th>2</th>
      <td>ADC Service (Storage Nodes\nonly)</td>
      <td>Whether this Storage Node will run the Administrative Domain\nController (ADC) service. The ADC service keeps track of the\nlocation and availability of grid services. At least three Storage\nNodes at each site must include the ADC service. You can’t add\nthe ADC service to a node after it is deployed.\n• Select Yes if the Storage Node you are replacing includes the\nADC service. Because you can’t decommission a Storage\nNode if too few ADC services would remain, this ensures that a\nnew ADC service is available before the old service is\nremoved.\n• Select Automatic to let the system determine whether this\nnode requires the ADC service.\nADC quorum. Learn about the</td>
    </tr>
    <tr>
      <th>3</th>
      <td>Storage Grade (Storage Nodes\nonly)</td>
      <td>Use the Default storage grade, or select the custom storage grade\nyou want to assign to this new node.\nStorage grades are used by ILM storage pools, so your selection\ncan affect which objects will be placed on the Storage Node.</td>
    </tr>
  </tbody>
</table></div>
                   <hr>
                