
                   <p style="text-align: center">
                       <b>Title:</b> c. For the Select binding mode step, specify the binding mode. The binding mode controls how the
endpoint is accessed using any IP address or using specific IP addresses and network interfaces.<br>
                       <b>Bounding box:</b> x1=267, y1=1196, x2=1533, y2=2022
                   </p>
                   <div align="center"><table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>0</th>
      <th>1</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>Mode</td>
      <td>Description</td>
    </tr>
    <tr>
      <th>1</th>
      <td>Global (default)</td>
      <td>Clients can access the endpoint using the IP address of any Gateway\nNode or Admin Node, the virtual IP (VIP) address of any HA group on\nany network, or a corresponding FQDN.\nUse the Global setting (default) unless you need to restrict the\naccessibility of this endpoint.</td>
    </tr>
    <tr>
      <th>2</th>
      <td>Virtual IPs of HA\ngroups</td>
      <td>Clients must use a virtual IP address (or corresponding FQDN) of an HA\ngroup to access this endpoint.\nEndpoints with this binding mode can all use the same port number, as\nlong as the HA groups you select for the endpoints don’t overlap.</td>
    </tr>
    <tr>
      <th>3</th>
      <td>Node interfaces</td>
      <td>Clients must use the IP addresses (or corresponding FQDNs) of\nselected node interfaces to access this endpoint.</td>
    </tr>
    <tr>
      <th>4</th>
      <td>Node type</td>
      <td>Based on the type of node you select, clients must use either the IP\naddress (or corresponding FQDN) of any Admin Node or the IP address\n(or corresponding FQDN) of any Gateway Node to access this endpoint.</td>
    </tr>
  </tbody>
</table></div>
                   <hr>
                