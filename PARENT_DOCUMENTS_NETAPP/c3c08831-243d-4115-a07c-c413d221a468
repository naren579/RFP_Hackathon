
                   <p style="text-align: center">
                       <b>Title:</b> Some binding modes are available for either client endpoints or management interface endpoints. All
modes for both endpoint types are listed here.<br>
                       <b>Bounding box:</b> x1=184, y1=500, x2=1566, y2=1483
                   </p>
                   <div align="center"><table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>0</th>
      <th>1</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>Mode</td>
      <td>Description</td>
    </tr>
    <tr>
      <th>1</th>
      <td>Global (default for client\nendpoints)</td>
      <td>Clients can access the endpoint using the IP address of any Gateway Node or\nAdmin Node, the virtual IP (VIP) address of any HA group on any network, or a\ncorresponding FQDN.\nUse the Global setting unless you need to restrict the accessibility of this\nendpoint.</td>
    </tr>
    <tr>
      <th>2</th>
      <td>Virtual IPs of HA groups</td>
      <td>Clients must use a virtual IP address (or corresponding FQDN) of an HA group\nto access this endpoint.\nEndpoints with this binding mode can all use the same port number, as long as\nthe HA groups you select for the endpoints don’t overlap.</td>
    </tr>
    <tr>
      <th>3</th>
      <td>Node interfaces</td>
      <td>Clients must use the IP addresses (or corresponding FQDNs) of selected node\ninterfaces to access this endpoint.</td>
    </tr>
    <tr>
      <th>4</th>
      <td>Node type (client\nendpoints only)</td>
      <td>Based on the type of node you select, clients must use either the IP address\n(or corresponding FQDN) of any Admin Node or the IP address (or\ncorresponding FQDN) of any Gateway Node to access this endpoint.</td>
    </tr>
    <tr>
      <th>5</th>
      <td>All Admin Nodes (default\nfor management\ninterface endpoints)</td>
      <td>Clients must use the IP address (or corresponding FQDN) of any Admin Node\nto access this endpoint.</td>
    </tr>
  </tbody>
</table></div>
                   <hr>
                