
                   <p style="text-align: center">
                       <b>Title:</b> 6/25/24, 11:34 AM
Content Platform System Management Help<br>
                       <b>Bounding box:</b> x1=185, y1=80, x2=1516, y2=1276
                   </p>
                   <div align="center"><table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>0</th>
      <th>1</th>
      <th>2</th>
      <th>3</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>Property\nname</td>
      <td>Data\ntype</td>
      <td>Description</td>
      <td>Notes</td>
    </tr>
    <tr>
      <th>1</th>
      <td>localHost</td>
      <td>String</td>
      <td>Identifies the local system for the replication link. Valid values are:\nThe domain name for the remote system for the link to use for communication with the local system (as that name is\nknown to the remote system), in either of these formats:\nreplication.hcp-domain-\nname\nreplication.admin.\nhcp-domain-name\nOne or more comma-separated IP addresses of storage nodes in the local system (as those addresses are known to\nthe remote system)\nFor more information about these values, see the description of the remoteHost property.\nTypically, you specify this property on a PUT request to create a link only if the system on which you’re creating the link uses\nnetwork address translation (NAT) for communication with the other system.</td>
      <td>This property is optional on\na PUT request to create a\nreplication link.</td>
    </tr>
    <tr>
      <th>2</th>
      <td>localPort</td>
      <td>Integer</td>
      <td>Specifies the port on which the local system for the replication link listens for data from the remote system. The default is\n5748.\nTypically, you specify a different port only if other port usage makes it necessary.</td>
      <td>This property is optional on\na PUT request to create a\nreplication link.</td>
    </tr>
    <tr>
      <th>3</th>
      <td>remoteHost</td>
      <td>String</td>
      <td>Identifies the remote system for the replication link. Valid values are:\nThe domain name of the remote system, in either of these formats:\nreplication.hcp-domain-\nname\nreplication.admin.\nhcp-domain-name\nhcp-domain-name must be the name of the domain associated with the network that’s selected for replication\non the remote system. The second format is required if the domain for the replication network is shared with\nother networks.\nOne or more comma-separated IP addresses of storage nodes in the remote system. These must be the node IP\naddresses in the network that’s selected for replication on the remote system.\nThe local system for the link transmits data only to the nodes identified by the domain name or IP addresses you\nspecify. Therefore, you should specify IP addresses only if you have a compelling reason to do so (for example, HCP\nis not using DNS, or you need to reduce the processing load on some number of nodes).</td>
      <td>This property is required on\na PUT request to create a\nreplication link.</td>
    </tr>
    <tr>
      <th>4</th>
      <td>remotePort</td>
      <td>Integer</td>
      <td>Specifies the port on which the remote system for the replication link listens for data from the local system. The default is\n5748.\nTypically, you specify a different port only if other port usage makes it necessary.</td>
      <td>This property is optional on\na PUT request to create a\nreplication link.</td>
    </tr>
  </tbody>
</table></div>
                   <hr>
                