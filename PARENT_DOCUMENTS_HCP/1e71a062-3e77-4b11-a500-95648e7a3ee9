
                   <p style="text-align: center">
                       <b>Title:</b> The table below describes the HTTP status codes that have specific meaning for this request.<br>
                       <b>Bounding box:</b> x1=185, y1=1467, x2=1516, y2=2123
                   </p>
                   <div align="center"><table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>0</th>
      <th>1</th>
      <th>2</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>Code</td>
      <td>Meaning</td>
      <td>Description</td>
    </tr>
    <tr>
      <th>1</th>
      <td>201</td>
      <td>Created</td>
      <td>HCP successfully copied the object. If versioning is enabled and an object with the same name already exists, HCP created a\nnew version of the object.</td>
    </tr>
    <tr>
      <th>2</th>
      <td>304</td>
      <td>Not Modified</td>
      <td>One of:\nA request to copy an object specified an header, and the ETag of the source X-HCP-Copy-Source-If--Match\nobject matches the value in the header.\nA request to copy an object specified header, and the source object X-HCP-Copy-Source-If-Modified-Since\nchange time is at or before the time specified in the header.\nA request to store a new version of an existing object specified an header, and the change time of If-Modified-Since\nthe current version is at or before the time specified in the header.</td>
    </tr>
    <tr>
      <th>3</th>
      <td>400</td>
      <td>Bad Request</td>
      <td>The request was not valid. These are some, but not all, of the possible reasons:\nThe URL in the request is not well-formed.\nEither the source or target namespace does not exist.\nThe target namespace has custom metadata XML checking enabled, and the copied object includes custom metadata\nthat is not well-formed XML.\nIf the request that causes this error included an header with the value HCP creates X-HCP-MetadataDirective ALL,\nan empty object before it returns the error. To resolve this issue, you can either:\nFix the custom metadata, and retry the request.</td>
    </tr>
  </tbody>
</table></div>
                   <hr>
                