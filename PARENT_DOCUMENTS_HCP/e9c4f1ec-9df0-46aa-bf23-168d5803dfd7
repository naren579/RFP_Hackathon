
                   <p style="text-align: center">
                       <b>Title:</b> Except when requesting all available information, the entry is required for operation-based queries. It can contain any combination of the entries listed in the table below. operation<br>
                       <b>Bounding box:</b> x1=185, y1=305, x2=1516, y2=1046
                   </p>
                   <div align="center"><table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>0</th>
      <th>1</th>
      <th>2</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>Entry</td>
      <td>Valid values</td>
      <td>Description</td>
    </tr>
    <tr>
      <th>1</th>
      <td>count</td>
      <td>One of:\n-1, to request all operation records that meet the\nquery criteria\nA positive integer</td>
      <td>Specifies the maximum number of operation records to return per request.\nIf you omit this entry, HCP returns up to ten thousand operation records per request.</td>
    </tr>
    <tr>
      <th>2</th>
      <td>lastResult</td>
      <td>N/A</td>
      <td>Specifies the last record returned by the previous query. Use this entry in paged queries\nto request additional results after an incomplete response. Omit this entry if you are not\nusing a paged query or if this is the first request in a paged query.</td>
    </tr>
    <tr>
      <th>3</th>
      <td>objectProperties</td>
      <td>A comma separated list of object properties.</td>
      <td>Requests specific object property values for each object entry in the query results.\nIf the request body contains both the and entries, HCP verbose objectProperties\nreturns only the object URL, version ID, operation type, and change time and the\ninformation specified in the entry. objectProperties</td>
    </tr>
    <tr>
      <th>4</th>
      <td>systemMetadata</td>
      <td>N/A</td>
      <td>Specifies the properties to use as the query criteria.</td>
    </tr>
    <tr>
      <th>5</th>
      <td>verbose</td>
      <td>One of:\ntrue\nReturn all object properties.\nfalse\nReturn only the object URL, version ID, operation,\nand change time.</td>
      <td>Specifies whether to return complete metadata for each operation record in the result set\nor to return only the object URL, version ID, operation type, and change time (true) (fal\nse).\nThe default is false.\nIf the query request body contains both the and entries, verbose objectProperties\nHCP returns only the object URL, version ID, operation type, and change time and the\ninformation specified in the entry. objectProperties</td>
    </tr>
  </tbody>
</table></div>
                   <hr>
                