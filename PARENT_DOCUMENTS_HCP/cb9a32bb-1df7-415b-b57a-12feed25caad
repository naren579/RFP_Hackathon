
                   <p style="text-align: center">
                       <b>Title:</b> 6/25/24, 11:34 AM
Content Platform System Management Help<br>
                       <b>Bounding box:</b> x1=185, y1=80, x2=1516, y2=2124
                   </p>
                   <div align="center"><table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>0</th>
      <th>1</th>
      <th>2</th>
      <th>3</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>Property</td>
      <td>Data type</td>
      <td>Description</td>
      <td>Notes</td>
    </tr>
    <tr>
      <th>1</th>
      <td></td>
      <td></td>
      <td>MB, GB, or TB (for example, 10.25 TB). The minimum\nvalue is 1 (one) for GB and .01 for TB.</td>
      <td></td>
    </tr>
    <tr>
      <th>2</th>
      <td>id</td>
      <td>String</td>
      <td>Specifies the system-supplied unique ID for the tenant.\nHCP generates this ID automatically when you create a\ntenant.</td>
      <td>This property is not valid on a POST request. It is returned\nonly by a verbose GET request.</td>
    </tr>
    <tr>
      <th>3</th>
      <td>maxNamespacesPer User</td>
      <td>integer</td>
      <td>For an HCP tenant, specifies the maximum number of\nnamespaces that can be owned at one time by any given\nuser.\nValid values are zero through 10,000. The default is 100.</td>
      <td>This property is not valid for the default tenant.</td>
    </tr>
    <tr>
      <th>4</th>
      <td>name</td>
      <td>String</td>
      <td>Specifies the name of the tenant. HCP derives the\nhostname for the tenant from this name. The hostname is\nused in URLs for access to the tenant and its\nnamespaces.\nThe name of the default tenant is always Default.</td>
      <td>This property is not valid on a POST request. It is returned\nonly by a verbose GET request for an HCP tenant.\nThis property is not returned by any GET request for the\ndefault tenant.\nThe tenant name is used in the URL for access to the\ntenant.</td>
    </tr>
    <tr>
      <th>5</th>
      <td>namespaceQuota</td>
      <td>String</td>
      <td>For an HCP tenant, specifies the number of namespaces\nHCP reserves for the tenant out of the total number of\nnamespaces the system can have. This is also maximum\nnumber of namespaces the tenant can own at any given\ntime.\nPossible values are:\nIntegers in the range 1 (one) through the current\nnumber of namespaces available for allocation.\nThe number of available namespaces is equal to\n10,000 minus the number of namespaces currently\nallocated to HCP tenants, minus the number of\nnamespaces currently owned by HCP tenants\nwithout a quota, minus one for the default\nnamespace, if it exists. If any tenants are above\nquota, the number of excess namespaces is also\nsubtracted from the number of available\nnamespaces.\n</td>
      <td>This property is not valid on a POST request. It is returned\nonly by a verbose GET request.\nThis property is not valid for the default tenant.</td>
    </tr>
    <tr>
      <th>6</th>
      <td>replicationConfiguration\nEnabled</td>
      <td>Boolean</td>
      <td>For an HCP tenant, specifies whether the tenant is eligible\nfor replication. Possible values are:\ntrue\nThe tenant is eligible for replication.\nfalse\nThe tenant is not eligible for replication.</td>
      <td>This property is not valid on a POST request. It is returned\nonly by a verbose GET request.\nThis property is not valid for the default tenant. If the HCP\nsystem supports replication, directories in the default\nnamespace are automatically eligible for replication.</td>
    </tr>
    <tr>
      <th>7</th>
      <td>searchConfiguration\nEnabled</td>
      <td>Boolean</td>
      <td>For an HCP tenant, specifies whether the tenant can\nenable and disable search for the namespaces it owns.\nPossible values are:\ntrue\nThe tenant can enable and disable search for its\nnamespaces.\nfalse\nThe tenant cannot enable or disable search for its\nnamespaces.</td>
      <td>This property is not valid on a POST request. It is returned\nonly by a verbose GET request.\nThis property is not valid for the default tenant.</td>
    </tr>
    <tr>
      <th>8</th>
      <td>snmpLoggingEnabled</td>
      <td>Boolean</td>
      <td>Specifies whether tenant log messages are sent to the\nSNMP managers specified at the HCP system level. Valid\nvalues are:\ntrue\nTenant log messages are sent to SNMP\nmanagers.\nfalse\nTenant log messages are not sent to SNMP\nmanagers.\nThe default is false.</td>
      <td></td>
    </tr>
    <tr>
      <th>9</th>
      <td>softQuota</td>
      <td>Integer</td>
      <td>For an HCP tenant, specifies the percentage point at\nwhich HCP notifies the tenant that free storage space is</td>
      <td>This property is not valid on a POST request. It is returned\nonly by a verbose GET request.</td>
    </tr>
  </tbody>
</table></div>
                   <hr>
                