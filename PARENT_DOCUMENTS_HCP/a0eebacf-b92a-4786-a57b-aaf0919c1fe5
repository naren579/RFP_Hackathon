
                   <p style="text-align: center">
                       <b>Title:</b> 6/25/24, 11:33 AM
Content Platform System Management Help<br>
                       <b>Bounding box:</b> x1=185, y1=80, x2=1516, y2=991
                   </p>
                   <div align="center"><table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>0</th>
      <th>1</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>Element</td>
      <td>Description</td>
    </tr>
    <tr>
      <th>1</th>
      <td></td>
      <td>Type: Enum (GET, PUT, HEAD, POST, DELETE)\nAncestor: CORSRule</td>
    </tr>
    <tr>
      <th>2</th>
      <td>AllowedOrigin</td>
      <td>Origins that you want to allow cross-origin requests from. Each CORSRule must identify at least one origin and one method.\nThe origin value can include at most one wildcard character "*", for example, http://*.example.com. Alternatively, you can specify the wildcard character by\nitself to enable all origins to send cross-origin requests.\nType: String\nAncestor: CORSRule</td>
    </tr>
    <tr>
      <th>3</th>
      <td>AllowedHeader</td>
      <td>Optional. List of headers that are allowed in a preflight OPTIONS request through the Access-Control-Request-Headers header. This element can contain at\nmost one wildcard character "*". Each header name in the Access-Control-Request-Headers header must have a corresponding entry in the CORSRule. The\nserver will send only the allowed headers that were requested in a response.\nType: String\nAncestor: CORSRule</td>
    </tr>
    <tr>
      <th>4</th>
      <td>MaxAgeSeconds</td>
      <td>Optional. Maximum time, in seconds, that the browser can cache a preflight OPTIONS response for a specified resource. By caching the response, the\nbrowser does not have to send preflight requests to the server within the MaxAgeSeconds time window if repeated requests (same origin, HTTP method, and\nresource) are issued.\nA CORSRule can have at most one MaxAgeSeconds element.\nType: Integer (seconds)\nAncestor: CORSRule</td>
    </tr>
    <tr>
      <th>5</th>
      <td>ExposeHeader</td>
      <td>Optional. One or more response headers that customers can access from their applications, for example, from a JavaScript XMLHttpRequest object.\nYou add one ExposeHeader element in the rule for each header. This element restricts the response headers that are accessible by the client.\nType: String\nAncestor: CORSRule</td>
    </tr>
  </tbody>
</table></div>
                   <hr>
                