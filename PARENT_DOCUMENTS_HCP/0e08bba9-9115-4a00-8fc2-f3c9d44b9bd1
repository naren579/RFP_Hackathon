
                   <p style="text-align: center">
                       <b>Title:</b> The object entry is required for object-based requests. It must contain the entry and can contain any combination of the other entries listed in the table below. query<br>
                       <b>Bounding box:</b> x1=185, y1=1469, x2=1516, y2=2123
                   </p>
                   <div align="center"><table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>0</th>
      <th>1</th>
      <th>2</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>Entry</td>
      <td>Valid values</td>
      <td>Description</td>
    </tr>
    <tr>
      <th>1</th>
      <td>query</td>
      <td>A query expression</td>
      <td>Specifies the query criteria. This entry is required.</td>
    </tr>
    <tr>
      <th>2</th>
      <td>content\nProperties</td>
      <td>One of:\ntrue\nReturn information for all content\nproperties.\nfalse\nDo not return any information on\ncontent properties.</td>
      <td>Returns information about the content properties available for use in queries.\nThe default is false.\nTo return only content properties, specify a entry with a value of 0. count</td>
    </tr>
    <tr>
      <th>3</th>
      <td>count</td>
      <td>One of:\n-1, to request all results\n0, to request a response that\nincludes only the object count and,\nif requested, content properties and\nfacets.\nAn integer between one and 10,000\n•</td>
      <td>Specifies the maximum number of results to return.\nIf you omit this entry, HCP returns a maximum of one hundred results.\nHCP responds significantly faster to a request for all results when the request is for\nbasic information only (that is, the value of the entry is (or defaults to) verbose false\nand the entry is omitted). Additionally, a request for all results that objectProperties\nincludes the entry with a value of or that includes the verbose true objectProperties\nentry may not return all the expected results due to a connection timeout.</td>
    </tr>
    <tr>
      <th>4</th>
      <td>facets</td>
      <td>A comma-separated list of zero or more of:</td>
      <td>Requests summary information for the returned values of the specified object properties.</td>
    </tr>
  </tbody>
</table></div>
                   <hr>
                